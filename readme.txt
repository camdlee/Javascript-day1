1. What is Javascript?
-- A programming language that turns a static website into a dynamic website. (allows user interaction)

2. Writing JS code in the Developer Console.

3. Writing JS code in your own IDE (VS Code)
-- Create index.html
-- Create main.js
-- link main.js in index.html at the bottom of your body
-- Verify it works!

4. Semicolons are read and used by the compiler to distinguish between separate statements 
so that statements do not leak into other parts of the code. 
NOTE: Your code will work with or without semicolons, BUT it is good practice and alleviates any weird behaviors.

5. Data Types in Javascript
-- Null
-- Undefined
-- Boolean
-- Number (Integers and Decimals)
-- BigInt
-- String
-- Symbol

6. Using Formatted Strings in JS
-- Use backticks(``) instead of quotes(''), wrap your parameter in curly braces ({}), 
-- and add dollar sign(`${parameter}`).